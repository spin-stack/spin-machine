name: Release

# One tarball, one version, and the version names a day rather than a semantic promise.
#
# CalVer — v20260909.01 — because that is what a release of this repository is: the machine
# as it stood on a date. There is no API here to promise compatibility about, and the one
# thing a version could promise — that templates still match — is decided by the
# fingerprint of the artefacts and not by a number anybody chose. A major version bump
# would say something this repository cannot mean.
#
# The `.01` is the run within the day, because two releases on one day is normal when a
# flag is being chased.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version, e.g. v20260909.01. Left empty, one is generated for today."
        required: false
        type: string

permissions:
  contents: write
  packages: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    # QEMU and the kernel are each tens of minutes from cold, on a runner with two cores,
    # and this job builds both plus the image. The GitHub Actions cache makes a warm run
    # far shorter; the ceiling is for the cold one.
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          # The whole history and the tags: the version below counts the tags already
          # taken today, and a shallow clone would count zero and hand out .01 twice.
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Task
        uses: go-task/setup-task@v2

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v4

      # Three sources, in order: what the caller typed, the tag that triggered this, or a
      # new one for today. The generated form is deliberately not `date +%s` or a commit
      # hash — it has to be readable, sortable, and the same shape as one typed by hand.
      # The caller's input arrives through the environment and not through ${{ }} in the
      # script body. Interpolation happens before the shell sees the line, so a version
      # containing a quote or a semicolon would be pasted into the script rather than
      # assigned to a variable — and it is validated below, which is after that would have
      # mattered.
      - name: Decide the version
        id: version
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_VERSION:-}" ]; then
            version="$INPUT_VERSION"
            origin="given by the caller"
          elif [ "${{ github.ref_type }}" = "tag" ]; then
            version="${{ github.ref_name }}"
            origin="the tag that triggered this run"
          else
            day=$(date -u +%Y%m%d)
            # Count what today already has. `git tag -l` on a fetch-depth:0 checkout sees
            # every tag; the sequence is two digits so that v20260909.02 sorts after .01
            # and both sort before v20260910.01, as strings, everywhere.
            taken=$(git tag -l "v${day}.*" | wc -l)
            version=$(printf 'v%s.%02d' "$day" "$((taken + 1))")
            origin="generated for today (${taken} already taken)"
          fi

          case "$version" in
            v[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].[0-9][0-9]) ;;
            *) echo "::error::'$version' is not vYYYYMMDD.NN" >&2; exit 1 ;;
          esac

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Releasing $version — $origin"

      # `task release` builds all three artefacts and packs them, ending in hack/release,
      # which refuses to produce a tarball with anything missing. A release that installs
      # and then fails for want of an option ROM is worse than no release.
      - name: Build the release
        env:
          VERSION: ${{ steps.version.outputs.version }}
          QEMU_CACHE_FROM: type=gha
          QEMU_CACHE_TO: type=gha,mode=max
          KERNEL_CACHE_FROM: type=gha
          KERNEL_CACHE_TO: type=gha,mode=max
          IMAGE_CACHE_FROM: type=gha
          IMAGE_CACHE_TO: type=gha,mode=max
        run: task release QEMU_JOBS=4 KERNEL_NPROC=4

      # The machine's identity, in the log and in the release notes. These are the numbers
      # that decide whether a template still matches, so "why did every template in the
      # fleet stop matching?" is answered by diffing this against the previous release.
      - name: What this release is
        id: manifest
        run: |
          set -euo pipefail
          name="spin-machine-${{ steps.version.outputs.version }}-linux-x86_64"
          manifest="_output/release/${name}/usr/share/spin-stack/machine.env"
          cat "$manifest"
          {
            echo 'notes<<EOF'
            echo 'The machine as it stood on this date. These checksums decide which'
            echo 'templates a host may restore from: a release in which any of them moved'
            echo 'is a different machine, and every template taken against the previous'
            echo 'one stops matching.'
            echo
            echo '```'
            cat "$manifest"
            echo '```'
            echo EOF
          } >> "$GITHUB_OUTPUT"

      # Tagged here when the run generated its own version, so that the next run's count
      # of "tags already taken today" is right and the tarball has a commit behind it.
      - name: Tag the commit
        if: github.ref_type != 'tag'
        run: |
          set -euo pipefail
          git tag "${{ steps.version.outputs.version }}"
          git push origin "${{ steps.version.outputs.version }}"

      - name: Publish
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }}
          body: ${{ steps.manifest.outputs.notes }}
          files: |
            _output/release/*.tar.gz
            _output/release/*.tar.gz.sha256
